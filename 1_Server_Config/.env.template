# --- Database Passwords ---
PG_PASS=change_this_to_strong_password

# --- Authentik Secrets ---
# 아무거나 아주 길게 입력 (예: asdfjkl12345...)
AUTH_SECRET=generate_random_string_here_very_long

# --- Grafana Password ---
GRAFANA_PASS=admin_password_change_me

# --- Cloudflare Tunnel ---
# Cloudflare 웹사이트 -> Zero Trust -> Tunnels 에서 생성 후 복사한 토큰
CLOUDFLARE_TOKEN=eyJhIjoi...
#main system가동
#docker-compose up -d

#보안 시스템 가동
#docker-compose -f docker-compose-auth.yml up -d